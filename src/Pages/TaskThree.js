import React from 'react';

const TaskThree = () => {
    return (
        <div>
            
        </div>
    );
};

export default TaskThree;



// Task 3 : (Moderate)
// You have to create two databases (student and teacher) in your
// database server. You have to store basic information about students
// & teachers in respective databases. You have to fetch the student
// name from the student database and teacher name from the
// teacher database and then print names in the console.
// Hint: Create multi connection database.
// Note: API http://localhost:3000/api/upcloud/users/